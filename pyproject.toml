[build-system]
build-backend = 'mesonpy'
requires = ['meson-python', 'pyyaml', 'pybind11', 'jinja2', 'ply']

[project]
name = 'rpi-libcamera'
version = '0.1a12'
description = 'libcamera python bindings'
readme = 'README.md'

[project.urls]
Source = "https://github.com/krleon/pylibcamera"
"RPi libcamera Source" = "https://github.com/krleon/libcamera"

[tool.meson-python.args]
setup = [
    '-Drevision=framos',
    '-Drepository=https://github.com/krleon/libcamera.git',
    '-Dversion=unknown'
]

[tool.flit.module]
name = "libcamera"
